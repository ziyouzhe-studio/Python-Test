# 提出空白，用变量表示一个人的名字，在其开头和结尾都包含一些空白。至少使用"\t"、"\n"各一次
# 然后分别使用lstrip()、rstrip()、strip()进行处理
# strip()删除两端空白
# lstrip()删除左侧空白
# rstrip()删除右侧空白
name = " tom "
print(name)
print(name.lstrip())
print(name.rstrip())
print(name.strip())